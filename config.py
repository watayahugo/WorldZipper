# The name of the folder / directory that needs autozipped
folder = {"folderName": "world"}
# The full path of where you would like the folder to be backed up to
# NOTE: make sure this is a \\ for Windows or a / if UNIX OS
destination = {"directoryName": ""}
